# Android release packet — 2026-09-15

## Actual artifacts

`npm run android:build` builds bundled web assets, syncs Android and runs `assembleDebug bundleRelease lintDebug testDebugUnitTest`. Installed Homebrew Java 21 and Android SDK 36 work independently of the pending Xcode license. No license was accepted. Device enumeration returned no devices; there is no emulator installation in this SDK.

- `artifacts/android/2026-09-15/wakestate-debug.apk`: installable **debug-signed** development build, not a production release.
- `artifacts/android/2026-09-15/wakestate-release-unsigned.aab`: actual compiled **unsigned** release bundle; not upload-ready.
- `artifacts/android/2026-09-15/SHA256SUMS`: artifact hashes.
- `artifacts/android/2026-09-15/lint-results-debug.html`: native lint report.

These local binaries are ignored by git. Standard Gradle outputs remain under `android/app/build/outputs/`. AAPT verified package `com.wakestate.app`, versionCode 1, versionName 1.0, minSdk 24 and targetSdk 36. APK certificate verification identifies Android Debug; jarsigner confirms the AAB is unsigned. App resources now use a teal crescent sleep/wake vector for adaptive/legacy launchers and launch screen, including themed-icon support. Device visual verification and store PNG artwork/screenshots remain open.

Verification: native compilation, Android unit tasks and native lint passed (0 errors / 15 warnings, including retained template resources and dependency/tooling advisories). TypeScript and all 22 tests passed, including complete backup/import, failed validation, concurrency, feedback malformed/expiry and request body tests. These tests do not substitute for device backup/share/restore testing. Earlier browser failure checks are in LAUNCH.md; no production feedback submissions were made here.

## Publisher and signing blocker

Coordinating Admin task reports the existing Play developer account is closed for inactivity and cannot be reactivated. No active publisher account, owned package ID or upload key is verified. `com.wakestate.app` stays provisional. Do not create a new ID or upload under an assumed identity. Admin owns account resolution, including health-app organization-account classification and D-U-N-S requirements. Do not use a personal-account shortcut; see [official account-type guidance](https://support.google.com/googleplay/android-developer/answer/13634885?hl=en). Once publisher/package ownership is confirmed, supply a publisher-controlled upload key through secure local signing configuration, sign the release AAB, and verify the resulting certificate. Never sign release with the debug key. No passwords/keys belong in this repo.

Remote check: fetched origin successfully using the bundled fallback git (system git hits Xcode agreement). Before this pass, local main `cacb6f5` was four commits ahead and zero behind origin/main; previous work had not reached remote. No push is included.

## Store fields prepared

- Name: WakeState
- Short description: Track wake states, sleep, events, and medications. See your own patterns.
- Full description and reviewer instructions: LAUNCH.md, Store listing draft.
- App access: no login; journal is local. Optional feedback requires network.
- Health functionality: personal sleep/wake, symptom and medication journaling; no diagnosis, treatment, wearable data, Health Connect or clinical claims. Select actual applicable health categories in the live form.
- Permissions observed in compiled APK: INTERNET and AndroidX app-scoped signature receiver permission; no camera, location, contacts or health sensor permission.
- Data safety draft: journal remains local; user-initiated exports leave through selected destinations; optional feedback sends role/section/category/text to Supabase, with request metadata visible to providers. Do not blanket-select “no data collected.” Confirm provider logs, retention and processing before answering collected/shared classifications.
- Privacy/support URLs: not verified; no owned production hostname or publisher-approved contact was supplied. `public/privacy.html` is an in-app notice, not a completed policy. Requires publisher/contact, retention/deletion procedure, provider processing and rights disclosures at a public HTML URL. Admin owns publisher facts.
- Screenshots and listing icon/feature graphic: still required from verified device/UI renders; do not represent desktop captures as Android screenshots.

Current official sources checked September 15:
- [Android build/signing workflow](https://developer.android.com/build/building-cmdline)
- [Health-app policy and public HTML privacy notice requirement](https://support.google.com/googleplay/android-developer/answer/16679511?hl=en)
- [Health declaration](https://support.google.com/googleplay/android-developer/answer/14738291?hl=en)
- [Data safety definitions](https://support.google.com/googleplay/android-developer/answer/10787469?hl=en)
- [Target SDK policy](https://support.google.com/googleplay/android-developer/answer/11926878?hl=en)

## Precisely staged backend and privacy gates

No auth/RLS configuration changed. Run `verify-feedback-security.sql` with authorized backend access and retain policy/grant results without feedback content. Verify anon cannot select or insert feedback, authenticated non-admin cannot read it, and the service-role function alone can insert valid submissions. Check source migration expectations against deployed policy results; source is not deployment evidence.

SEC-01 remains open. Before public feedback: replace the answer-bearing base64 token with random server-stored single-use challenge IDs, hashed answers bound to ID/expiry, and atomic consumption; enforce durable rate limits at challenge issuance AND submission across instances, using a gateway-validated address-derived HMAC bucket rather than trusting arbitrary forwarded headers. Use a separate server-only secret; document bucket/challenge TTL cleanup and log retention. Mutation, service-secret provisioning and RLS rollout need explicit operator approval. A CAPTCHA-like arithmetic question is not sufficient bot prevention by itself.

Acceptance tests for the authorized backend rollout: missing/invalid/expired token rejected; concurrent replay produces at most one accepted submission; restart does not reset limits; wrong answers consume attempt budget; oversize UTF-8/chunked body returns 413; bad JSON returns 400; missing datastore fails closed; logs exclude submitted text and raw IP. Deploy existing bounded-body handler changes with the replacement and verify in a test environment first.

At the actual web host, verify HTTPS and prepare nosniff, no-referrer, frame-ancestors 'none', object-src 'none', base-uri 'self', script-src 'self', and connect-src restricted to self plus the exact configured Supabase origin. Current React/chart inline styles require allowance under style-src. Test worker/font/image sources and feedback before enforcing CSP. Hosting origin and deployment authority are not established, so no untested production header configuration was applied.

## Minimum remaining handoff

1. Admin resolves active publisher, package ownership, upload signing key and support/privacy facts.
2. Backend operator completes the staged security rollout and verifies production policy/headers/retention.
3. Connect an authorized disposable Android device: install debug APK, test offline entries, force-close persistence, export/share/restore, cancellation, data deletion, back navigation, large text and launcher/splash. Do not overwrite an existing journal installation without a verified backup.
4. Produce device screenshots/store artwork, sign AAB, run internal testing, then obtain explicit store submission approval.
